//  there are multiple import export styles
import SiteContext, { useSite } from "./SiteContext";
export { default as AuthContext, useAuth } from "./AuthContext";

export {
    SiteContext,
    useSite
}
